<div class="c-balance-card {{ $priority }}">
    <div class="e-title">
        {{ $slot }}
        <span class="e-title-text">{{ $title }}</span>
    </div>

    <p class="e-amount">¥{{ $amount }}</p>
</div>
